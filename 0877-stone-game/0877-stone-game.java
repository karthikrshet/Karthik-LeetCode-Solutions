class Solution {
    public boolean stoneGame(int[] piles) {
        // Alice always wins because she can control the game strategy 
        // to always pick the optimal set of piles due to even length.
        return true;
    }
}